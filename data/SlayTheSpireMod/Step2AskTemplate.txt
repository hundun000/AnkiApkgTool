数据结构java描述如下：

public static class ConfuseResult {
    String id;
    List<String> confusedFurigana;
}

public static class ConfuseInput {
    String id;
    String show;
    String furigana;
}

我的目的是制作日语考题，届时show字段将展示给考生，考生需要在confusedFurigana和正确答案之间选择。你来帮我出题目中的错误选项。每次我输入一个List<ConfuseInput>，你输出一个List<ConfuseResult>。不要输出json以外的内容，确保json的格式合法。注意检查输出和输入的id完全对应。

样例输入：
[{
    "id": "uuid1234",
    "show": "正しい",
    "furigana": "ただしい"
}]

样例输出：
[{
    "id": "uuid1234",
    "confusedFurigana": ["たつしい", "たどしい", "たたしい"]
}]

输入的furigana字段表示日语单词的正确假名读音; show字段是日语单词的带汉字的形式。

输出的confusedFurigana表示生成的读音的错误选项。注意满足以下要求:

- 从题干 `show`（如“正しい”）中提取所有连续的平假名片段（比如“しい”）。生成的每个候选读法，必须按顺序、完整地保留这些片段，且不引入额外的片段或改变顺序。如样例中，错误选项只应该对“正=ただ”这部分做出混淆，“しい”部分保持一致。例如混淆为“正=たた”最终输出为“たたしい”。
- 是不含汉字的假名读音。
- 不要重复，不要和正确答案相同。
- 数量不超过4个。

以下是我的输入：